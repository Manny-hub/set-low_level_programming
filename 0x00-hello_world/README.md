# C - Hello, World

This project introduces the basics of the C programming language.

## Task 0: Preprocessor

The `0-preprocessor` script runs a C file through the C preprocessor and saves the preprocessed output into a file named `c`.

The C file is specified using the `CFILE` environment variable.

### Usage

```bash
export CFILE=main.c
./0-preprocessor